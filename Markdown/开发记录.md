## 2026年7月1日
整体思路
工程入口在 [main.c (line 79)]。
启动流程是：
开 I-Cache / D-Cache。
HAL_Init() 初始化 HAL 和系统节拍。
SystemClock_Config() 配系统时钟和外设时钟。
初始化 GPIO、SPI、UART：
MX_GPIO_Init()、MX_SPI3_Init()、MX_USART1/2/3_UART_Init()、MX_SPI2_Init()、MX_SPI4_Init()。
初始化业务驱动：
ADS7951_Init()、RS422_Init()、SSI422_Init()。
上电后先读一次 ADC 和 SSI422。
主循环里目前持续读取 U16/U18 两路 SSI422 编码器值。
所以现在主程序还是偏“底层验证/驱动搭建阶段”，业务逻辑没有复杂状态机，重点是先把外设链路跑通。
模块分层
现在可以理解成三层：
第一层是 CubeMX/HAL 生成层：
[gpio.c (line 41)]：配置片选、方向控制、DE/RE、TX_EN 等 GPIO。
[spi.c (line 35)]：配置 SPI2、SPI3、SPI4。
[usart.c (line 35)]：配置 USART1、USART2、USART3。
[main.h (line 61)]：集中定义 GPIO 引脚宏。
第二层是设备驱动层：
[ads7951.c (line 12)]：两片 ADS7951 ADC。
[ssi422.c (line 32)]：两路 SSI422 编码器读取。
[rs422.c (line 225)]：两路 RS422 串口通信。
第三层是应用层：
[main.c (line 116)]：调用各驱动读数，目前主要保存到全局变量，方便 DAPLink/Keil 调试看。
ADS7951 模块
ADS7951 是 ADC 采样模块，走 SPI3。
核心思路：
SPI3 是公共总线。
U6 和 U8 两片 ADS7951 用不同片选脚区分。
驱动里把“构造命令、拉低片选、SPI 交换、拉高片选、取 12 位结果”封装起来。
对外提供：ADS7951_U6_ReadChannel()
ADS7951_U8_ReadChannel()
ADS7951_U6_ReadAll()
ADS7951_U8_ReadAll()

这里用了一个比较好的封装方式：底层通用函数 ADS7951_ReadRaw() 接收片选端口和通道号，上层再封装成 U6/U8 专用接口。这样以后加第三片 ADC，也不用重写读数逻辑。
SSI422 模块
SSI422 是编码器读数模块，走 SPI2 和 SPI4。
当前对应关系是：
U16 -> SPI4
U18 -> SPI2
核心思路：
每个 SSI422 口都有一组 SPI 句柄、DE、RE 控制脚。
用 SSI422_PortConfig 表描述硬件资源。
读数前先切到编码器读取模式。
SPI 发送 dummy 数据，同时接收编码器返回值。
SSI422_ReadBits() 会把多个 16 位 word 拼成一个 32 位原始值，再按实际 bit 数右对齐。
主循环现在一直做：
ssi_u16_status = SSI422_U16_ReadBits(16U, &ssi_u16_value);
ssi_u18_status = SSI422_U18_ReadBits(16U, &ssi_u18_value);
所以当前程序运行时，U16/U18 是持续刷新的。
RS422 模块
RS422 是目前做得最完整的通信驱动。
当前硬件对应关系：
U9 -> USART1
U10 -> USART3
这里之前排查后已经修正过：U10 实际接的是 UART3，不是 UART2。
RS422 驱动的核心思路是：
每个端口有独立配置：UART 句柄、TX_EN 控制脚、发送有效电平。
每个端口有独立状态：接收环形缓冲、发送环形缓冲、忙标志。
接收使用 HAL_UART_Receive_IT() 单字节中断接收。
收到一个字节后进环形缓冲，再重新挂下一字节接收。
发送使用中断方式逐字节发送，发送完成后自动继续取下一个字节。
发完后自动切回接收模式。
对外主要接口是：
RS422_Transmit_IT()：非阻塞发送。
RS422_Transmit()：阻塞等待发送完成。
RS422_Available()：看接收缓存里有多少字节。
RS422_Read()：从接收缓存取数据。
RS422_ClearRx()：清接收缓存。
这个模块已经不是简单 HAL 包装了，它有自己的收发队列和诊断变量，后面接协议会比较方便。
目前状态
现在工程已经完成了几个关键阶段：
ADC 驱动已封装。
SSI422 双路读数已封装，并在主循环持续读取。
RS422 U9/U10 自发自收测试已通过。
自测代码已经从 main.c 移除，恢复成正常对外程序。
U10 串口映射已修正到 USART3。
VS Code IntelliSense 配置也已补上。

## 2026年7月3日

### RS422 通讯协议开发

本次在原有 `rs422.c` 底层收发驱动上新增了一层 RS422 协议封装，代码文件为：

- `Core/Inc/rs422_protocol.h`
- `Core/Src/rs422_protocol.c`

协议帧格式确定为：

| 字段 | 长度 | 说明 |
| --- | --- | --- |
| 包头 | 2 字节 | 固定为 `0x0F 0xF0`，对应需求中的 `0x0FF0` |
| ID | 1 字节 | 板子不同对外接口号 |
| 数据类型 | 1 字节 | 只允许 `0x00`、`0xFF`、`0x55`、`0xAA` |
| 收发指示 | 1 字节 | `0` 表示发送，`1` 表示接收 |
| 数据长度 | 1 字节 | 固定为 `128` |
| 数据区 | 128 字节 | 实际业务数据 |
| 校验位 | 1 字节 | 从 ID 到数据区做 8 位累加校验 |

完整协议帧长度为 `135` 字节。这样做的好处是包头清晰、长度固定、解析简单，后面在串口字节流中只需要先同步包头，再按固定长度收满一帧即可。

### 协议层主要接口

`rs422_protocol.c` 对外提供的核心函数如下：

- `RS422_ProtocolBuildFrame()`：按照协议格式组装 135 字节完整数据帧。
- `RS422_ProtocolSend()`：组帧后调用底层 `RS422_Transmit()` 发送。
- `RS422_ProtocolDecodeFrame()`：检查包头、数据类型、方向、长度和校验位，并解析为结构体。
- `RS422_ProtocolPoll()`：从底层 RS422 接收缓存中轮询字节流，自动寻找包头并解析完整协议包。
- `RS422_ProtocolClearStats()`：清空指定端口的协议统计。
- `RS422_ProtocolGetStats()`：读取指定端口的接收统计。

接收统计中保留了以下信息，方便在 Keil Watch 中直接观察：

- 接收总字节数
- 正确帧数
- 错误帧数
- 错误字节数
- 包头同步丢弃字节数
- 误码率，单位为 ppm

### RS422 测试与示例代码

在 `main.c` 中保留了两个默认关闭的调试开关：

```c
#define RS422_PROTOCOL_TEST_ENABLE           0U
#define RS422_UART1_EXAMPLE_ENABLE           0U
```

`RS422_PROTOCOL_TEST_ENABLE` 用于 J12/J13 互连时做 U9/U10 协议收发测试，正常对外程序保持 `0U`，不会主动发送测试帧。

`RS422_UART1_EXAMPLE_ENABLE` 用于单独演示串口1的协议收发调用。串口1在当前工程中对应 `RS422_PORT_U9`，也就是 `USART1 / U9 / J12`。打开后会周期发送一帧协议数据，同时轮询接收端口中的协议包。

串口1示例调用方式如下：

```c
RS422_ProtocolSend(RS422_PORT_U9,
                   RS422_UART1_EXAMPLE_ID,
                   (uint8_t)RS422_PROTOCOL_TYPE_55,
                   rs422_uart1_example_tx_data,
                   RS422_UART1_EXAMPLE_TX_TIMEOUT_MS);

RS422_ProtocolPoll(RS422_PORT_U9, &rs422_uart1_example_rx_packet);
```

这部分只是示例入口，后面正式业务逻辑可以直接照这个方式调用协议层接口。

### 串口发送和接收调用

当前业务层推荐优先调用协议层接口，因为协议层已经处理了 `0x0FF0` 包头、135 字节固定帧长、数据类型检查和累加校验。

发送一帧 128 字节业务数据：

```c
uint8_t tx_data[RS422_PROTOCOL_DATA_LENGTH] = {0U};

/* 填充 tx_data[0..127] 后发送，U9 对应 USART1/J12。 */
HAL_StatusTypeDef tx_status =
    RS422_ProtocolSend(RS422_PORT_U9,
                       1U,
                       (uint8_t)RS422_PROTOCOL_TYPE_55,
                       tx_data,
                       200U);
```

接收时放在主循环里轮询：

```c
RS422_ProtocolPacket rx_packet = {0U};
RS422_ProtocolStatus rx_status;

rx_status = RS422_ProtocolPoll(RS422_PORT_U9, &rx_packet);
if (rx_status == RS422_PROTOCOL_STATUS_OK)
{
    /* 收到完整协议帧后，在这里使用 rx_packet.id、rx_packet.data_type、rx_packet.data。 */
}
```

如果只是临时调试裸串口字节，不走协议层，也可以直接调用底层 RS422 驱动：

```c
uint8_t raw_tx[] = {0x11U, 0x22U, 0x33U};
uint8_t raw_rx[32] = {0U};
uint16_t raw_rx_len;

/* 阻塞等待发送完成。 */
(void)RS422_Transmit(RS422_PORT_U9, raw_tx, sizeof(raw_tx), 100U);

/* 非阻塞发送，只把数据放入发送队列。 */
(void)RS422_Transmit_IT(RS422_PORT_U9, raw_tx, sizeof(raw_tx));

/* 查询接收缓存，有数据就读出来。 */
if (RS422_Available(RS422_PORT_U9) > 0U)
{
    raw_rx_len = RS422_Read(RS422_PORT_U9, raw_rx, sizeof(raw_rx));
}
```

端口选择保持当前映射：`RS422_PORT_U9` 是 `USART1 / U9 / J12`，`RS422_PORT_U10` 是 `USART3 / U10 / J13`。

### 硬件和串口关系确认

本次确认并修正了 RS422 两路接口关系：

- U9 对应 `USART1`，外部连接器为 J12。
- U10 对应 `USART3`，外部连接器为 J13。

之前排查时确认 U10 实际不是接 UART2，而是接 UART3，所以代码中已经按 `USART3` 处理。

J12 和 J13 按 RS422 互连后，协议接收结果已经能在 Keil Watch 中看到正确数据，接收状态为 `RS422_PROTOCOL_STATUS_OK`，误码率统计为 0。

### 工程配置

Keil 工程文件 `MDK-ARM/shuziliangceshi.uvprojx` 已加入：

- `rs422_protocol.h`
- `rs422_protocol.c`

这样在 Keil5 中打开工程后，协议层文件会直接参与编译。

另外新增 `.vscode/c_cpp_properties.json`，用于解决 VS Code 中 C/C++ 插件找不到 HAL/CMSIS 头文件的问题。该配置只影响 VS Code IntelliSense，不影响 Keil 编译。

### 编译和提交状态

本次提交前使用 Keil5 编译验证，结果为：

```text
0 Error(s), 0 Warning(s)
```

代码已提交并上传到 GitHub，最新提交为：

```text
20fe3f8 Add RS422 protocol support
```

仓库地址：

```text
https://github.com/piaolianxue/project_code.git
```

## 2026年7月8日

### RS422 串口1和串口3示例同时打开

本次在 `main.c` 中打开了串口1和串口3的 RS422 协议收发示例：

```c
#define RS422_UART1_EXAMPLE_ENABLE           1U
#define RS422_UART3_EXAMPLE_ENABLE           1U
```

串口1仍然对应 `RS422_PORT_U9 / USART1 / U9 / J12`，保持原来的示例功能：每 1 秒发送一帧协议数据，同时在主循环里调用 `RS422_ProtocolPoll()` 轮询接收。

串口3新增同样的示例功能，对应 `RS422_PORT_U10 / USART3 / U10 / J13`。主要调用方式如下：

```c
RS422_ProtocolSend(RS422_PORT_U10,
                   RS422_UART3_EXAMPLE_ID,
                   (uint8_t)RS422_PROTOCOL_TYPE_AA,
                   rs422_uart3_example_tx_data,
                   RS422_UART3_EXAMPLE_TX_TIMEOUT_MS);

RS422_ProtocolPoll(RS422_PORT_U10, &rs422_uart3_example_rx_packet);
```

串口3新增的 Keil Watch 观察变量为：

- `rs422_uart3_example_tx_data`
- `rs422_uart3_example_rx_packet`
- `rs422_uart3_example_stats`
- `rs422_uart3_example_tx_status`
- `rs422_uart3_example_rx_status`
- `rs422_uart3_example_tx_count`

串口1示例数据仍然使用递增字节 `0x00..0x7F`，串口3示例数据使用递减字节，便于在接收端区分两路数据来源。

### UART7 上位机通讯接口

本次新增 UART7 作为上位机通讯接口，对应原理图引脚：

- `PE7`：`UART7_RX`
- `PE8`：`UART7_TX`

UART7 默认配置为 `115200 8N1`，并支持上位机通过协议动态配置 UART7 波特率，以及控制 RS422 串口1/串口3示例是否运行。UART7 自身保持常开，用于持续接收上位机命令。新增代码文件：

- `Core/Inc/host_comm.h`
- `Core/Src/host_comm.c`

上位机帧格式按图纸实现：

| 字段 | 长度 | 说明 |
| --- | --- | --- |
| 帧头 | 1 字节 | 固定 `0xEE` |
| 指令 | 1 字节 | 命令码 |
| 参数 | 0 到 1024 字节 | 指令参数，数值按十六进制传输 |
| 帧尾 | 4 字节 | 固定 `0xFF 0xFC 0xFF 0xFF` |

当前定义的指令码如下：

| 指令 | 方向 | 参数 | 说明 |
| --- | --- | --- | --- |
| `0x01` | 上位机到板子 | 4 字节小端 `uint32_t` | 设置 UART7 波特率 |
| `0x02` | 上位机到板子 | 2 字节：目标串口、使能值 | 设置 RS422 串口1/串口3示例使能 |
| `0x81` | 板子到上位机 | 22 字节统计数据 | 上传通讯统计 |

`0x02` 的参数说明：

| 偏移 | 长度 | 说明 |
| --- | --- | --- |
| 0 | 1 字节 | 目标串口，`1` 表示串口1/U9/USART1，`3` 表示串口3/U10/USART3，`0xFF` 表示两路一起 |
| 1 | 1 字节 | 使能值，`0` 关闭，非 `0` 打开 |

`0x81` 统计数据参数区格式：

| 偏移 | 长度 | 说明 |
| --- | --- | --- |
| 0 | 4 字节 | `rx_total_bytes`，接收总字节数 |
| 4 | 4 字节 | `tx_total_bytes`，发送总字节数 |
| 8 | 4 字节 | `error_bytes`，错误字节数 |
| 12 | 4 字节 | `error_rate_ppm`，误码率，单位 ppm |
| 16 | 1 字节 | `uart1_enabled`，串口1示例使能状态 |
| 17 | 1 字节 | `uart3_enabled`，串口3示例使能状态 |
| 18 | 4 字节 | `baudrate`，UART7 当前波特率 |

板子收到设置波特率或串口1/串口3使能命令后，会立即回传一帧 `0x81` 统计；主循环中也会每 1 秒周期上传一次统计。误码率计算方式为：

```text
error_rate_ppm = error_bytes * 1000000 / rx_total_bytes
```

用于 Keil Watch 观察的主要变量：

- `host_comm_rx_total_bytes`
- `host_comm_tx_total_bytes`
- `host_comm_error_bytes`
- `host_comm_error_rate_ppm`
- `host_comm_baudrate`
- `host_comm_uart1_enabled`
- `host_comm_uart3_enabled`
- `host_comm_last_command`
- `host_comm_last_tx_status`
- `host_comm_start_receive_status`
